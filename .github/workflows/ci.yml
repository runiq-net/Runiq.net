name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'badges/tests.json'
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-pack:
    name: Build, test and pack
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:0.8.0-pg17
        env:
          POSTGRES_DB: runiq_rag_dev
          POSTGRES_USER: runiq_dev
          POSTGRES_PASSWORD: runiq_dev_only
        ports:
          - 54329:5432
        options: >-
          --health-cmd "pg_isready -U runiq_dev -d runiq_rag_dev"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Runiq.AI.slnx

      - name: Verify test reporting
        shell: pwsh
        run: ./scripts/test-update-test-badge.ps1

      - name: Build
        run: dotnet build Runiq.AI.slnx --no-restore -c Release -v:minimal

      - name: Test
        id: test
        continue-on-error: true
        run: dotnet test Runiq.AI.slnx --no-build -c Release -v:minimal --logger trx --results-directory TestResults

      - name: Update test badge
        id: badge
        if: always()
        shell: pwsh
        env:
          TEST_OUTCOME: ${{ steps.test.outcome }}
        run: |
          [xml]$solution = Get-Content Runiq.AI.slnx -Raw
          $expected = @($solution.Solution.Folder | Where-Object Name -eq '/tests/' | ForEach-Object { $_.Project }).Count
          ./scripts/update-test-badge.ps1 -ResultsDirectory TestResults -OutputPath badges/tests.json -TestOutcome $env:TEST_OUTCOME -ExpectedResultCount $expected -SummaryPath $env:GITHUB_STEP_SUMMARY

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            TestResults/**/*.trx
            badges/tests.json
          retention-days: 14

      - name: Commit test badge
        if: always() && !cancelled() && steps.badge.outcome == 'success' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Badge updates only push from main so pull request builds validate the JSON
          # without trying to write to contributor branches or forks.
          git add badges/tests.json
          if git diff --cached --quiet -- badges/tests.json; then
            echo "Test badge is already up to date."
            exit 0
          fi

          git commit -m "chore: update test badge [skip ci]"
          git push

      - name: Fail if tests failed
        if: steps.test.outcome == 'failure'
        run: exit 1

      - name: Pack
        run: dotnet pack Runiq.AI.slnx --no-build -c Release -o artifacts/packages

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: runiq-packages
          path: artifacts/packages/*.nupkg
